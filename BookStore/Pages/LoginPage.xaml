<Page x:Class="BookStore.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookStore.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход в систему"
      Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="700"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" TextBlock.Foreground="White">
                <TextBlock Text="📚" FontSize="80" HorizontalAlignment="Center" Margin="0 0 0 20"/>
                <TextBlock Text="Книжный магазин"
                           FontFamily="{StaticResource TitleFont}"
                           FontSize="32"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" Padding="40">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Вход в систему" Style="{StaticResource PageTitle}" Margin="0 0 0 30"/>

                <TextBlock Text="Логин" Margin="0 0 0 5" FontWeight="SemiBold"/>
                <TextBox x:Name="EmailTextBox" Style="{StaticResource ModernTextBox}" Margin="0 0 0 20"/>

                <TextBlock Text="Пароль" Margin="0 0 0 5" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBox}" Margin="0 0 0 30"/>

                <Button x:Name="LoginButton"
                        Content="Войти"
                        Style="{StaticResource PrimaryButton}"
                        FontSize="16"
                        Click="LoginButton_Click"/>

                <TextBlock x:Name="ErrorTextBlock"
                           TextWrapping="Wrap"
                           FontSize="13"
                           Foreground="#E74C3C"
                           Margin="0 20 0 0"
                           Visibility="Collapsed"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
